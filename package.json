{
  "name": "playwright-browser-testing",
  "version": "1.0.0",
  "description": "Splunk → Playwright browser tests - manual",
  "scripts": {
    "test": "HEADED=true npx playwright test",
    "test:headless": "npx playwright test",
    "report": "npx playwright show-report src/report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
